Compute the fact that the pmf average and standard error is made between three blocks of 50ns (section101-section150, section151-section200, section201-section250)
Write an alternative script get_pmf2.py (and extract_metadata2.py if necessary) that uses the last 50ns of a 100ns length trajectory (section51-section100) and calculate the average and standard error through the difference between the two leaflets (z-positive vs z-negative, residue 1 vs residue 2).

